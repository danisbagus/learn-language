const findFibonacci = (numbs, lastValue = 0) => {
  const result = [];
  let last = lastValue;
  if (numbs === 0) {
    return lastValue;
  } else {
    lastValue = 
  }

  return;
};

console.log("result", findFibonacci(5));
